pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "Run build"
        sh 'chmod +x build_script.sh'
        sh './build_script.sh'
      }
    }
    stage('Test') {
      steps {
        echo "Run tests"
      }
    }
  }
}
